{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start NLP Server",
            "type": "python",
            "request": "launch",
            "module": "uvicorn",
            "cwd": "${workspaceFolder}/nlp-service",
            "args": [
                "app.api:app",
                "--reload"
            ]
        }
    ]
}
